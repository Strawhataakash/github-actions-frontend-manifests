apiVersion: v1
kind: Service
metadata:
  name: github-actions-frontend
  namespace: frontend-staging
  labels:
    app: github-actions-frontend
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: github-actions-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
